<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules xmlns:android="http://schemas.android.com/apk/res/android">
    <cloud-backup>
        <!-- Specify what to include or exclude from cloud backup here -->
        <!-- Example:
        <include android:resource="@xml/my_backup_rules" />
        -->
    </cloud-backup>

    <!--
    <device-transfer>
        <include android:resource="@xml/my_transfer_rules" />
        <exclude android:resource="@xml/exclude_rules" />
    </device-transfer>
    -->
</data-extraction-rules>
